import { Capacitor } from "@capacitor/core"; import { Haptics, ImpactStyle } from "@capacitor/haptics"; const isNative = Capacitor.isNativePlatform(); export function lightImpact() { if (!isNative) return; Haptics.impact({ style: ImpactStyle.Light }).catch(() => {}); } export function mediumImpact() { if (!isNative) return; Haptics.impact({ style: ImpactStyle.Medium }).catch(() => {}); }
